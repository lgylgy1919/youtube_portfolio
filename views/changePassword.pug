extends layouts/main

block content
    .form-container
        form(action=`/users${routes.changePassword}`, method="post")
            input(type="passworrd", name="oldPassword", placeholder="Current Password")
            input(type="passworrd", name="newPassword", placeholder="New Password")
            input(type="passworrd", name="newPassword1", placeholder="Verify New Password")
            input(type="submit", value="Change Password")